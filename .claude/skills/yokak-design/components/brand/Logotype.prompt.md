Official YÖKAK / THEQC logo, rendered from the supplied artwork (never redrawn).

```jsx
<Logotype lang="en" size="md" />                 // colour THEQC lockup
<Logotype lang="tr" variant="reversed" />        // white YÖKAK on brand blue
<Logotype variant="mono" height={32} />          // black, custom height
```

Props: `lang` en/tr, `variant` primary/reversed/mono, `size` sm/md/lg or explicit `height`.
Set `assetBase` (or `window.YOKAK_ASSET_BASE`) when the page is not at the design-system root.
Seal artwork: `assets/logos/seal-5yr-full-accreditation.png` (gold, 5-year full accreditation).
