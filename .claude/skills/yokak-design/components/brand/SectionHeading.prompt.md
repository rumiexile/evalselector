Section-opener heading: eyebrow label, bold title, and the signature gradient rule — the recurring motif that opens sections across the identity system.

```jsx
<SectionHeading eyebrow="About THEQC" title="Quality assurance in higher education" />
```

Variants: `align` (left/center).
