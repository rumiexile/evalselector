Rectangular filter/category tag for programme fields, subject areas, and multi-select filters.

```jsx
<Tag selected>Engineering</Tag>
<Tag onRemove={() => {}}>Medicine</Tag>
```

Variants: `selected` toggles brand-blue fill; `onRemove` adds a dismiss ×.
