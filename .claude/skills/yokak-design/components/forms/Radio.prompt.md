Circular radio input with label — used for single-choice questions in evaluation forms.

```jsx
<Radio name="tier" label="Full accreditation" checked={tier==='full'} onChange={() => setTier('full')} />
```
