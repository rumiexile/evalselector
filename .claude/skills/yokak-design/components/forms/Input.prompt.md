Text field for forms — institution search, applicant details, report metadata.

```jsx
<Input label="Institution name" placeholder="e.g. Boğaziçi University" />
<Input label="Contact email" error="Enter a valid email address" />
```

Variants: `size` (sm/md), `hint`, `error` (danger border + message).
